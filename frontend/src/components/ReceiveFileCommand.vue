<template>
    <v-card outlined>
        <v-card-title>
            ReceiveFile
        </v-card-title>

        <v-card-text>
            <String label="Filename" v-model="value.filename" :editMode="editMode"/>
            <String label="Path" v-model="value.path" :editMode="editMode"/>
            <String label="ReceivedBy" v-model="value.receivedBy" :editMode="editMode"/>
        </v-card-text>

        <v-card-actions>
            <v-spacer></v-spacer>
            <v-btn
                    color="primary"
                    text
                    @click="receiveFile"
            >
                ReceiveFile
            </v-btn>
            
            <v-btn
                    color="primary"
                    text
                    @click="close"
            >
                Close
            </v-btn>
        </v-card-actions>
    </v-card>

</template>

<script>
   
    export default {
        name: 'ReceiveFileCommand',
        components:{},
        props: {},
        data: () => ({
            editMode: true,
            value: {},
        }),
        created() {
            this.value.filename = '';
            this.value.path = '';
            this.value.receivedBy = '';
        },
        watch: {
        },
        methods: {
            receiveFile() {
                this.$emit('receiveFile', this.value);
            },
            close() {
                this.$emit('closeDialog');
            },
            change() {
                this.$emit('input', this.value);
            },
        }
    }
</script>

